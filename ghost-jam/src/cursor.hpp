#pragma once

FILDEF void        set_cursor (std::string _name);
FILDEF std::string get_cursor ();

FILDEF void show_cursor ();
FILDEF void hide_cursor ();

FILDEF void lock_cursor   ();
FILDEF void unlock_cursor ();
